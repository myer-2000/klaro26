# Run any service on its own, e.g. `docker compose up video`, or `up` for all.
# Postgres + Redis are included but commented — the scaffold uses in-memory
# adapters so it boots with zero external services. Uncomment to develop the
# production Store / Queue adapters.
services:
  video:
    build:
      context: .
      dockerfile: services/video/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - KLARO26_API_KEYS=klaro26_dev_key

  document:
    build:
      context: .
      dockerfile: services/document/Dockerfile
    ports:
      - "8081:8081"
    environment:
      - PORT=8081
      - KLARO26_API_KEYS=klaro26_dev_key

  markdown:
    build:
      context: .
      dockerfile: services/markdown/Dockerfile
    ports:
      - "8082:8082"
    environment:
      - PORT=8082
      - KLARO26_API_KEYS=klaro26_dev_key

  # postgres:
  #   image: pgvector/pgvector:pg16
  #   environment:
  #     POSTGRES_PASSWORD: postgres
  #     POSTGRES_DB: klaro26
  #   ports:
  #     - "5432:5432"

  # redis:
  #   image: redis:7
  #   ports:
  #     - "6379:6379"
